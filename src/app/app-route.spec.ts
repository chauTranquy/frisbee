/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {AppRoute} from './app-route';

describe('AppRoute', () => {
  it('should create an instance', () => {
    expect(new AppRoute()).toBeTruthy();
  });
});
